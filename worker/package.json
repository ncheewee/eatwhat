{
  "name": "eatwhat-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --experimental-default-type=module --test src/rank.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
